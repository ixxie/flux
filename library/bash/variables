#!/usr/bin/env bash
set -e

function writevar
{

    dir="$1"
    var="$2"
    typeset ${var}_name="$3"
    typeset ${var}_desc="$4"

    typeset -p $var{"","_name","_desc"} > $dir/$var".var"
    
}


function save
{
    file="$1"
    vars="${@:2}"
    declare -p $vars > $file
}